#!/usr/bin/env python3

import boto3

attrs = {
  'endpoint_url': 'http://localhost:800'
}

if len(sys.argv) == 2:
  if "prod" in i sys.argsv[1]:
  attrs = {}

dynamodb = boto3.client('dynamodb',**attrs)